// there are generally many data type but we take now some for understanding

#include <iostream>
using namespace std;

int main()
{

    // integer data type
    int a = 5;
    cout << a << endl;

    // float data type

    float b = 1.5;
    cout << b << endl;

    // double data type
    double db=1.256;
    cout<< db<<endl;

    // charater data type
    char ch = 'S';
    cout << ch << endl;

    // boolean data type
    bool x = false;
    cout << x << '\n';

    int size=sizeof(db);
    cout<<"the Size of a variable is:"<< size<<endl;
}